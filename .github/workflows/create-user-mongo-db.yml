name: Create User Mongo DB
on:
  push:
    branches:
      - feat/atlas-github-action
  workflow_dispatch:
    inputs:
      email:
        description: where to send the email
        required: true
